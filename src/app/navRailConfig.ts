/**
 * Nav rail visibility toggles — flip to `true` to restore hidden items.
 * Routes and placeholder pages stay registered; only the rail slot is hidden.
 */
export const SHOW_ADDONS_NAV = false;

/** Set to `true` to restore AI Agents nav slot and `/ai-agents` page (`AiAgentsPage.tsx`). */
export const SHOW_AI_AGENTS_PAGE = false;

/** When false, design-system shortcut is invisible but still clickable in the nav rail footer. */
export const SHOW_DESIGN_SYSTEM_NAV = false;

/** Amazon Athena connector wizard Step 3 — Map & Review Data (`AmazonAthenaMapReviewStep.tsx`). */
export const SHOW_ATHENA_CONNECTOR_STEP_3 = true;
